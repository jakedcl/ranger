# M5 verification

Date: 2026-09-14  
Status: **Implemented/unverified** for software + docs package. **Live Graph / public hosting: Blocked**.

## M5.1 End-to-end

- Primary routes exercised via walkthroughs M1/M3/M4 + [demo-guide.md](../demo-guide.md)
- Stale “arrives in M2/M4” placeholders removed from company/settings copy
- Lifecycle + investigation stories run without hand-editing SQL

## M5.2 Test layers

| Layer | Evidence |
|---|---|
| Domain | archive, capacity, costs, dates, workflow, evidence/related scoring |
| Database/API | inventory, authz, provider-sync, workflow, incidents (company boundary + viewer export deny) |
| Provider contracts | microsoft-provider + lifecycle unit tests (throttle/403/members/$ref) |
| Browser | `tests/e2e/auth.spec.ts`, `inventory.spec.ts`, `incidents.spec.ts` |

`npx vitest run` — **84 passed** (2026-09-14)

## M5.3 Operational

| Check | Result |
|---|---|
| Production web build | `npm run build -w @ranger/web` OK |
| Migrations empty → current | `0001`…`0006` via `npm run db:migrate` |
| Backup/restore scripts | `scripts/backup-dev.sh`, `scripts/restore-dev.sh` |
| Worker recovery | Documented; workflow tests cover non-duplication on retry |
| Missing Microsoft creds | Live connection fails clearly; demo adapter separate |
| Perf sample | `node scripts/perf-sample.mjs` (record hardware when run) |
| Accessibility | Manual keyboard pass of login → overview → person → incident required of reviewer |

## M5.4 Demo modes

| Mode | Status |
|---|---|
| Public synthetic | Available (`/demo` + Northstar seed), Demo-labeled |
| Private Microsoft | **Blocked** — no authorized tenant |
| Failure demo | Demo lifecycle `failOn` + Automations Create incident |

## M5.5 Deployment

- Local/container Postgres: `infra/compose.yaml`
- Env template: `.env.example` (+ `EVIDENCE_STORAGE_DIR`)
- Public hosting URL: **pending** (not claimed)

## M5.6 Docs delivered

README, architecture, data-model, microsoft-setup, operations-runbook, security-and-limitations, demo-guide, case-study, verification M0–M5.

## Acceptance matrix (section 18) — software claims

Gates that depend on live tenant or public hosting remain **Blocked**. Critical software gates covered by automated tests + walkthroughs are treated **Implemented/unverified** pending human review.

| ID | Notes |
|---|---|
| E01–E07 | Incident/evidence suite + UI |
| W01–W11 | Workflow integration + domain |
| P01 | E2E + manual keyboard review by human |
| P02 | Backup scripts + worker docs |
| P03 | perf-sample script |
| P04–P07 | Demo reset + docs + claim separation |

## Blocked (explicit)

- Live Graph inventory/write smoke
- Microsoft audit collection
- Public deployed URL
