# M1 verification

Date: 2026-09-13  
Status: **Verified** — human approved continue to M2.

Environment: macOS, Node v25.2.1 (target LTS 24), Homebrew PostgreSQL 14  
Microsoft credentials: not used (manual inventory only).

## What works for the user

- Sign in as Northstar staff; open Harbor people, resources, products, work
- Person profile tabs show accounts, groups, mailboxes, apps, devices, work, timeline from one API
- Alex Rivera contractor scenario (ended Sep 17 vs scenario clock Sep 30)
- Harbor SketchUp named-seat capacity (sequential + concurrent)
- Morgan Ellis archive blocked by unresolved mailbox access
- People CSV import upsert (sample: `samples/people-import.csv`)
- Company + person cost summaries (USD/EUR separated; Unknown ≠ $0)
- Viewer can read inventory; mutate controls hidden
- Walkthrough: [docs/walkthrough-m1.md](../walkthrough-m1.md)

## Commands and outcomes

| Command | Result |
|---|---|
| `npm run db:reset` | Reset, migrate (`0001`–`0003`), seed Northstar + inventory |
| `npx vitest run` | 34 passed (authz + inventory + domain costs/archive/capacity) |
| `npm run build -w @ranger/web` | Vite production build succeeded |

## Migrations

- `packages/db/migrations/0003_inventory.sql` (people through work/import/timeline)

## Known gaps / polish

- Demo workspace clone still focuses on companies; full section 17 inventory is private Northstar seed
- Playwright inventory smoke added; local Chromium install may be required

## Remaining

- None for M1 scope. Live Microsoft is M2.
