# M2 verification

Date: 2026-09-13  
Status: **Implemented/unverified** (fixture/demo path). **Live smoke: Blocked** — no authorized tenant credentials.

## What works

- Integrations screen: add **demo Microsoft adapter**, Sync now, sync history
- Demo sync upserts observed accounts/groups/memberships/SKU pools without deleting unobserved memberships
- Microsoft adapter refuses Graph without credentials; fixture/injectable fetch covered by unit tests
- Manual subscription pricing untouched by SKU pool observations
- Source and freshness badges on accounts, groups, person tabs, and resource details (demo source stays distinct)
- Worker schedules `ranger.provider-sync` every 15 minutes for `connected` providers
- Overview **Stale connections** queue counts error / never-succeeded / >24h / collection failures

## Commands

| Command | Result |
|---|---|
| `npx vitest run` | 56 passed |
| `npm run build -w @ranger/web` | OK |

## Live gate

Blocked until human provides authorized private test tenant + inventory app credentials (`MICROSOFT_TENANT_ID`, `MICROSOFT_INVENTORY_CLIENT_ID`, `MICROSOFT_INVENTORY_CLIENT_SECRET`). Do not mark live-verified from demo sync.

## Ready for human review

M2 demo/fixture path is ready for review. Do not start M3 until reviewed. Live Graph remains blocked.
