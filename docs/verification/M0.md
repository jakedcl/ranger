# M0 verification

Date: 2026-09-09  
Environment: macOS, Node v25.2.1 (target LTS is 24), Homebrew PostgreSQL 14.22  
Docker: not installed on this machine; `infra/compose.yaml` is provided for others.

## What works for the user

- Private staff sign-in at http://127.0.0.1:5173/login
- Isolated synthetic demo at http://127.0.0.1:5173/demo
- Company scope switcher, breadcrumbs, later-milestone empty states
- Admin sees Harbor, Cedar, and Summit; technician/viewer grants hide Summit
- Viewer cannot PATCH company IT notes
- Guessed Summit IDs return 404 in the API and an error in the UI
- Sign out returns to login

## Requirements completed

M0.1.1–M0.1.10, M0.2.1–M0.2.7, F01–F05, C08 (synthetic/demo path). Live Microsoft work is not in M0.

## Commands and outcomes

| Command | Result |
|---|---|
| `npm run db:migrate && npm run db:seed` | Applied `0001_better_auth.sql`, `0002_ranger_core.sql`; seeded Northstar |
| `npx vitest run` | 10 passed |
| `npx playwright test` | 6 passed (1440, 1024, 390) |
| `npm run build -w @ranger/web` | Vite production build succeeded |
| `curl http://127.0.0.1:4000/health` | `{"status":"ok"}` |
| `curl http://127.0.0.1:4000/ready` | `{"status":"ready","database":"ok"}` |

Browser check: demo workspace created, Overview showed Harbor and Cedar only, People showed the M0 empty state. Private admin/viewer journeys were exercised by Playwright rather than typing the seed password into the Cursor browser.

## Migrations

- `packages/db/migrations/0001_better_auth.sql`
- `packages/db/migrations/0002_ranger_core.sql`

## Decisions and limitations

- [docs/decisions/0001-architecture.md](../decisions/0001-architecture.md)
- Better Auth sign-in rate limiting is disabled in this local instance so verification can run; demo workspace creation remains capped at 20/hour.
- Docker Compose is documented but unused here.
- Microsoft credentials were not used. No live connector exists.

## Remaining blockers / next

- Human review of M0. Do not start M1 until approved.
- Microsoft tenant setup is blocked until credentials exist (M2).
