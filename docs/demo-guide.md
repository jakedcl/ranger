# Demo guide

## Visitor guided demo (recommended first look)

1. `npm run setup && npm run dev`
2. Open http://127.0.0.1:5173/demo
3. **Enter guided demo** — creates an isolated workspace with Harbor (Alex, Casey, SketchUp history, license incident), then starts the spotlight tour.
4. Next / Back / Esc. No staff password. No live Microsoft. Expires ~8 hours.

## Staff seed (full Northstar)

http://127.0.0.1:5173/login · `admin@northstar.example` + `SEED_STAFF_PASSWORD`  
Optional: check **Start guided tour after sign-in**, or use sidebar **Guided demo**.

## Reset

`npm run db:reset` — synthetic only.
